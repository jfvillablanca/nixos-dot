result
.direnv
.pre-commit-config.yaml
.nvim-baseline.path

# scratch / research dumps — never committed
*.local.md

# Claude Code per-user local overrides (settings.json is the shared, tracked one)
.claude/settings.local.json
